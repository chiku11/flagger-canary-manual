# Default values for charts.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

name: helloworld

version: v2

image:
  repository: docker.io/istio/examples-helloworld-v1
  pullPolicy: IfNotPresent

serviceAccount:
  name: helloworld
